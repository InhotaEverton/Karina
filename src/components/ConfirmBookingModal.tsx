import{CalendarDays,Clock,LoaderCircle,X}from'lucide-react';
export function ConfirmBookingModal({date,time,loading,onClose,onConfirm}:{date:Date|null;time:string;loading:boolean;onClose:()=>void;onConfirm:()=>void}){return <div className="fixed inset-0 z-[100] grid place-items-center bg-slate-950/50 p-5 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div className="w-full max-w-sm rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-sm font-semibold text-violet-600">Confirme seu horário</p><h2 id="confirm-title" className="mt-1 text-2xl font-bold">Deseja agendar?</h2></div><button onClick={onClose} disabled={loading} className="grid size-9 place-items-center rounded-full bg-slate-100 text-slate-500"><X size={18}/></button></div><div className="my-6 space-y-3 rounded-2xl bg-violet-50 p-4"><p className="flex items-center gap-3 font-semibold text-slate-800"><CalendarDays className="text-violet-600" size={20}/>{date?.toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'})}</p><p className="flex items-center gap-3 font-semibold text-slate-800"><Clock className="text-violet-600" size={20}/>{time}</p></div><p className="text-sm text-slate-500">Ao confirmar, este horário ficará reservado para você.</p><div className="mt-6 grid grid-cols-2 gap-3"><button disabled={loading} className="btn-secondary" onClick={onClose}>Voltar</button><button disabled={loading} className="btn-primary" onClick={onConfirm}>{loading&&<LoaderCircle className="animate-spin" size={17}/>}Sim, agendar</button></div></div></div>}

