import{useMemo,useState}from'react';import{ChevronLeft,ChevronRight}from'lucide-react';
const iso=(d:Date)=>{const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`};
export function MiniBookingCalendar({value,onChange}:{value:string;onChange:(date:string)=>void}){const initial=value?new Date(`${value}T12:00:00`):new Date();const[month,setMonth]=useState(new Date(initial.getFullYear(),initial.getMonth(),1));const cells=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1),start=new Date(first),daysInMonth=new Date(month.getFullYear(),month.getMonth()+1,0).getDate();start.setDate(1-first.getDay());const count=first.getDay()+daysInMonth<=35?35:42;return Array.from({length:count},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d})},[month]);const now=new Date();now.setHours(0,0,0,0);const canPrev=month.getFullYear()>now.getFullYear()||month.getMonth()>now.getMonth();return <div className="mini-calendar"><div className="mini-calendar-head"><button type="button" disabled={!canPrev} onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))}><ChevronLeft size={18}/></button><b>{month.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</b><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}><ChevronRight size={18}/></button></div><div className="mini-calendar-week">{['D','S','T','Q','Q','S','S'].map((d,i)=><span key={i}>{d}</span>)}</div><div className="mini-calendar-days">{cells.map((d,i)=>{const key=iso(d),other=d.getMonth()!==month.getMonth(),allowed=[2,3,4,5,6].includes(d.getDay())&&d>=now,active=value===key;return <button type="button" key={i} disabled={!allowed} onClick={()=>onChange(key)} className={`${other?'other ':''}${active?'active':''}`} aria-label={d.toLocaleDateString('pt-BR')}>{d.getDate()}</button>})}</div></div>}

