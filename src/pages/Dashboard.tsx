import { useEffect,useState } from 'react';import {ArrowDownRight,ArrowUpRight,CalendarDays,CircleDollarSign,Clock3,Wallet}from'lucide-react';import {Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import { getDashboard } from '../services/finance';import type { DashboardData } from '../types';import { ErrorState,Loading,Metric } from '../components/ui';import { money } from '../lib/format';
export function Dashboard(){const[data,setData]=useState<DashboardData|null>(null),[error,setError]=useState('');const load=()=>{setError('');getDashboard().then(setData).catch(e=>setError(e.message))};useEffect(load,[]);if(error)return <ErrorState message={error} retry={load}/>;if(!data)return <Loading/>;return <div><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h1 className="page-title">Visão geral</h1><p className="mt-1 text-sm text-slate-500">Seu resumo financeiro em tempo real.</p></div><button className="btn-secondary"><CalendarDays size={18}/>Mês atual</button></div><section className="grid grid-cols-2 gap-3 xl:grid-cols-5"><Metric label="Receitas no mês" value={data.monthIncome} icon={<ArrowUpRight/>} accent="emerald"/><Metric label="Despesas no mês" value={data.monthExpense} icon={<ArrowDownRight/>} accent="rose"/><Metric label="Lucro estimado" value={data.profit} icon={<CircleDollarSign/>}/><Metric label="Saldo em caixa" value={data.cashBalance} icon={<Wallet/>} accent="amber"/><Metric label="Comissões pendentes" value={data.pendingCommissions} icon={<Clock3/>}/></section><section className="mt-5 grid gap-5 xl:grid-cols-[1.7fr_1fr]"><div className="card p-5"><div className="mb-5"><h2 className="font-bold">Movimentação dos últimos 7 dias</h2><p className="text-sm text-slate-500">Receitas e despesas por competência</p></div><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data.chart}><defs><linearGradient id="r" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#7c3aed" stopOpacity={.25}/><stop offset="95%" stopColor="#7c3aed" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e2e8f0"/><XAxis dataKey="label" axisLine={false} tickLine={false}/><YAxis hide/><Tooltip formatter={(v)=>money.format(Number(v))}/><Area type="monotone" dataKey="receitas" stroke="#7c3aed" fill="url(#r)" strokeWidth={3}/><Area type="monotone" dataKey="despesas" stroke="#f43f5e" fill="transparent" strokeWidth={2}/></AreaChart></ResponsiveContainer></div></div><div className="card p-5"><h2 className="font-bold">Contas</h2><p className="mb-5 text-sm text-slate-500">Valores que precisam de atenção</p><div className="space-y-3"><div className="rounded-2xl bg-emerald-50 p-4"><p className="text-sm text-emerald-700">A receber</p><p className="mt-1 text-2xl font-bold text-emerald-800">{money.format(data.receivable)}</p></div><div className="rounded-2xl bg-rose-50 p-4"><p className="text-sm text-rose-700">A pagar</p><p className="mt-1 text-2xl font-bold text-rose-800">{money.format(data.payable)}</p></div><div className="flex justify-between rounded-2xl border p-4 text-sm"><span>Movimentações hoje</span><b>{data.movementCount}</b></div></div></div></section></div>}



